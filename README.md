# nagaizumi-ai-seminar-lp

長泉町 生成AI活用セミナーのLPと3問診断。

## Cloudflare Pages

- Framework preset: None
- Build command: none
- Build output directory: `/`
- Proposed custom domain: `ai.masaya-goto.com`

現在はプレビュー版のため、検索エンジンを `noindex` にしている。

本番公開時は `config.js` にUTAGE URLを設定し、`index.html` と `robots.txt` の検索制御を変更する。
